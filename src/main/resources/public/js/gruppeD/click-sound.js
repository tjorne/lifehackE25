/*function buttonClick  () {
    var audio = new Audio('../audio/click.mp3');
    audio.preload = 'auto';
    audio.volume = 0.5;

    function shouldPlay(target) {
        return !!target.closest('a, button, [role="button"], [onclick], input[type="button" i], input[type="submit" i], input[type="reset" i], label[for], summary, .guac-btn');
    }

    <a onClick="playSound(), ">

    document.addEventListener('pointerdown', function (e) {

        if (shouldPlay(e.target)) {
            audio.currentTime = 0;
            audio.play();
        }

    }, { passive: true });
};
*/

